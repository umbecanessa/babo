# Babo Desktop — local agent runtime with optional vision
#
# Used by the Electron desktop app's auto-setup (VenvManager).
# BYO OpenAI-compatible inference (local or remote). Consolidation sleep uses
# the LLM + memory DB only — no weight training on desktop.
#
# torch/transformers: tokenizer formatting and optional Moondream vision (CUDA).
# Excludes: unsloth, bitsandbytes, llama-cpp-python (optional: faster-whisper below).

# Server framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-multipart>=0.0.9

# Configuration & data models
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0

# Windows timezone support (zoneinfo needs this on Windows)
tzdata>=2024.1

# HTTP clients (OpenAI-compatible inference API)
httpx>=0.27.0
openai>=1.50.0

# Speech-to-text (local transcription when available)
faster-whisper>=1.0.0

# Model inference (Visual Cortex VLM + tokenizer for prompt formatting)
torch>=2.1.0
transformers>=4.40.0,<5.0.0
accelerate>=0.30.0
num2words>=0.5.13

# Moondream 2B dependencies (only loaded on CUDA >= 6 GB)
einops>=0.7.0
pyvips>=2.2.0
pyvips-binary>=8.16.0

# Web tools (search + fetch)
ddgs>=9.0.0
beautifulsoup4>=4.12.0
html2text>=2024.2.26

# Document parsing (agent read tool -- PDFs, Word, Excel, PPT, RTF)
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
striprtf>=0.0.26

# CLI utilities
rich>=13.0.0

# Browser tool
browser-use>=0.11.0
playwright>=1.48.0

# Visual Cortex (tiered VLM: Moondream / SmolVLM / FastVLM)
psutil>=5.9.0
certifi>=2024.2.2
# mlx-vlm>=0.20.0   # Optional: Apple Silicon only (FastVLM backend)

# Cloud Bridge APIs (for hybrid inference)
# anthropic>=0.40.0
