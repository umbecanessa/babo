{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "deployOutputs": ["."],
      "commands": [
        "...",
        "npm install --include=dev && npm run build:operator && npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start:railway"
  }
}
